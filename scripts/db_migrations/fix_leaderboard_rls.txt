-- FIX LEADERBOARD VISIBILITY
-- Copia todo este texto y pégalo en el SQL Editor de Supabase. Dale a RUN.

-- 1. Habilitar seguridad en la tabla (por si acaso)
ALTER TABLE public.game_sessions ENABLE ROW LEVEL SECURITY;

-- 2. PERMITIR QUE TODOS LEAN LA CLASIFICACIÓN (Esta es la clave que faltaba)
DROP POLICY IF EXISTS "Enable read access for all users" ON public.game_sessions;
CREATE POLICY "Enable read access for all users"
ON public.game_sessions
FOR SELECT
USING (true);

-- 3. PERMITIR QUE SE GUARDEN PUNTUACIONES (Insertar)
DROP POLICY IF EXISTS "Enable insert access for all users" ON public.game_sessions;
CREATE POLICY "Enable insert access for all users"
ON public.game_sessions
FOR INSERT
WITH CHECK (true);

-- 4. PERMITIR QUE SE ACTUALICEN RANKINGS (Update)
DROP POLICY IF EXISTS "Enable update access for users" ON public.game_sessions;
CREATE POLICY "Enable update access for users"
ON public.game_sessions
FOR UPDATE
USING (true);

-- 5. Dar permisos a la API (anon = usuarios sin registrar, authenticated = registrados)
GRANT ALL ON public.game_sessions TO anon;
GRANT ALL ON public.game_sessions TO authenticated;
GRANT ALL ON public.game_sessions TO service_role;

-- 6. Recargar configuración
NOTIFY pgrst, 'reload schema';
